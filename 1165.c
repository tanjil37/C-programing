#include <stdio.h>
int main() {
    int x,i,y;
    scanf("%d", &x);
    for ( i = 0; i < x; i++)
    {
        scanf("%d", &y);
        if ()
        {
            /* code */
        }
        
    }
    
return 0;
}